\chapter{Implementacja i~ewaluacja systemu detekcji}

System zaprojektowano jako narzędzie do~wsadowej analizy \textbf{plików wideo}
pod~kątem śladów pochodzenia generatywnego: widocznych watermarków oraz pośrednich
sygnałów sugerujących niewidoczne znakowanie wodne (sygnał proxy),
metadanych C2PA oraz artefaktów sygnałowych. Priorytetem projektowym był
\textbf{niski FPR} na~materiałach broadcastowych, ponieważ to~one najczęściej
zawierają dynamiczne nakładki mylone z~cechami AI.

Architektura ma~charakter modułowy: osobno działa warstwa GUI, orkiestracja
analizy i~zestaw detektorów. Taki podział umożliwia dokładanie nowych heurystyk
bez~przebudowy logiki fuzji. Rdzeń systemu zaimplementowano w~\textbf{Pythonie},
co~pozwoliło szybko iterować nad detektorami i~pipeline'em ewaluacyjnym.

\section{Język programowania i~biblioteki}

Rdzeń systemu zaimplementowano w~języku \textbf{Python~3}.
Graficzny interfejs użytkownika zbudowano z~wykorzystaniem biblioteki \textbf{PyQt6} ---
zestawu wiązań języka Python do~biblioteki Qt6, który pozwala tworzyć
natywnie wyglądające aplikacje okienkowe na~systemach Windows, macOS i~Linux.
Dzięki temu możliwe było przygotowanie w~pełni funkcjonalnej aplikacji desktopowej
z~tabelą wyników, mechanizmem kolejkowania plików, paskiem postępu,
podglądem klatki oraz obsługą raportów generowanych dla~każdego
analizowanego materiału.

W~warstwie przetwarzania wideo kluczową rolę pełnią biblioteki \textbf{OpenCV}
i~\textbf{NumPy}. OpenCV odpowiada za~odczyt klatek z~pliku wideo, operacje na~obrazie
(np.\ filtrowanie CLAHE, wyrównanie histogramu, detekcja krawędzi),
obliczanie przepływu optycznego metodą Farnebäcka oraz wstępne przygotowanie
danych dla~kolejnych etapów. NumPy służy do~wydajnych obliczeń
macierzowych i~operacji na~pikselach. Biblioteka \textbf{Pandas} umożliwia
agregację wyników z~wielu klatek i~zapis raportów do~plików~CSV.

Do~rozpoznawania tekstu w~klatkach wideo (watermarki widoczne, napisy,
\textit{lower thirds}) aplikacja korzysta z~silników~OCR.
Warstwa OCR wspiera kilka backendów lokalnych, jednak w~referencyjnym
benchmarku opisanym w~tej pracy wykorzystano \textbf{EasyOCR~1.7};
RapidOCR i~PaddleOCR pozostają opcjonalnymi alternatywami środowiskowymi.
W~warstwie analizy statystycznej sygnału wideo oraz przy~obliczaniu widma
częstotliwościowego klatek (analiza FFT) wykorzystywana jest biblioteka
\textbf{SciPy}.

Analiza proxy sygnałów związanych z~niewidocznym watermarkingiem
realizowana jest przez moduły sygnałowe wykorzystujące FFT, spójność
temporalną i~heurystyki fuzji. Niezależnie od tego walidacja metadanych
\textbf{C2PA} (\textit{Content Credentials}) odbywa się przez dedykowany
moduł \texttt{c2pa\_detector}, oparty na~publicznej specyfikacji
\textit{Coalition for Content Provenance and Authenticity}. W~części
badawczej uwzględniono ogólny kontekst systemów provenance-first jako
punkt odniesienia dla rozwiązań opartych na~znakach osadzanych
bezpośrednio w~procesie generacji, ale nie stanowiło to osobnego
kanału ilościowej ewaluacji.

\subsubsection*{Mapa kanałów watermarkingu i~proweniencji}
W~implementacji rozróżniono trzy kanały weryfikacji:
\begin{itemize}
  \item \textbf{Widoczny watermark} --- detekcja tekstu i~znaczników wizualnych
    przez OCR oraz analizę mediany temporalnej i~regionów o~niskim ruchu.
  \item \textbf{Proxy niewidocznego watermarkingu} --- sygnał wnioskowany pośrednio na podstawie anomalii residuum i~artefaktów sygnałowych
    (FFT, spójność temporalna); system nie dekoduje ukrytego ładunku informacyjnego
    ani nie gwarantuje identyfikacji konkretnego schematu znakowania, dlatego kanał ten należy interpretować jako heurystyczny sygnał pomocniczy.
  \item \textbf{Proweniencja} --- walidacja manifestu C2PA
    (\textit{Content Credentials}) i~metadanych pochodzenia pliku przez moduł \texttt{c2pa\_detector}.
\end{itemize}

% -------------------------------------------------------------------
\section{Architektura aplikacji i~GUI}

Interfejs użytkownika napisany w~PyQt6 pełni rolę warstwy sterującej
całym procesem analitycznym. Główne okno aplikacji podzielone jest
na~dwa panele rozdzielone ruchomym separatorem (\textit{splitter}): lewy panel
zawiera tabelę wyników, konsolę logów i~pasek postępu,
natomiast prawy panel prezentuje podgląd aktualnie analizowanej klatki wideo
oraz powiększony widok obszaru, w~którym wykryto watermark (\textit{Zoom}).

Tabela wyników zawiera sześć kolumn: nazwę pliku, typ materiału,
status detekcji AI, procentowy udział klatek z~wykryciem, status metadanych C2PA
oraz ścieżkę do~raportu~CSV. Dwukrotne kliknięcie wiersza otwiera
w~eksploratorze plików katalog wynikowy przypisany do~danego materiału wideo.

Obsługiwane formaty wideo to: \texttt{mp4}, \texttt{mov}, \texttt{avi},
\texttt{mkv} i~\texttt{webm}. Aplikacja jest ukierunkowana wyłącznie
na~analizę \textbf{materiałów filmowych}, nie obrazów statycznych,
co~odpowiada celowi pracy --- detekcji śladów
generatywnego pochodzenia w~plikach wideo, gdzie istotna jest zarówno
zawartość pojedynczej klatki, jak i~informacja temporalna między klatkami.

Pliki mogą być dodawane do~kolejki przez okno dialogowe, przez przeciągnięcie
i~upuszczenie (\textit{drag and drop}) lub rekurencyjne skanowanie całego folderu.
Użytkownik konfiguruje czułość OCR (parametr \textit{confidence}),
gęstość próbkowania klatek (\textit{sample rate}) oraz opcjonalny
tryb szczegółowej analizy dwufazowej.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.92\textwidth]{../photos/gui.png}
  \caption{Główne okno aplikacji (PyQt6) podczas wsadowej analizy plików wideo.
    Lewy panel zawiera tabelę z~wynikami detekcji (kolumny: plik, typ materiału,
    status AI, \% wykrytych klatek, status C2PA, ścieżka do~raportu~CSV).
    Prawy panel prezentuje aktualnie analizowaną klatkę oraz powiększony
    obszar wykrycia (\textit{Zoom}).}
  \label{fig:gui_main}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.92\textwidth]{../photos/gui_description.png}
  \caption{Interfejs aplikacji z~opisanymi elementami: (1)~przyciski dodawania
    plików i~folderów, (2)~pasek narzędzi / pasek postępu, (3)~tabela wyników
    z~kolorowymi statusami detekcji, (4)~konsola logów, (5)~panel podglądu klatki,
    (6)~panel Zoom z~powiększonym obszarem detekcji.}
  \label{fig:gui_desc}
\end{figure}

Rysunki~\ref{fig:gui_main} i~\ref{fig:gui_desc} dokumentują warstwę operacyjną
systemu: kolejkę plików, logi, podgląd klatki oraz dostęp do~artefaktów
diagnostycznych, które są wykorzystywane później przy analizie błędów.

\subsection*{Tryb CLI -- automatyzacja i~ewaluacja wsadowa}
\label{sec:cli}

Oprócz interfejsu graficznego aplikacja udostępnia tryb wiersza poleceń (CLI),
przeznaczony do~automatyzacji i~ewaluacji wsadowej bez~uruchamiania GUI.
Aplikacja wspiera dwa niezależne tryby uruchomienia:

\begin{itemize}
  \item \textbf{Tryb GUI} -- \texttt{python main.py} -- uruchamia pełny interfejs
    graficzny PyQt6 z~tabelą wyników, konsolą logów i~podglądem klatki;
  \item \textbf{Tryb CLI} -- \texttt{python kod/dataset/evaluate.py} -- uruchamia
    potok detekcji bezpośrednio z~wiersza poleceń, bez~żadnego interfejsu graficznego.
\end{itemize}

Tryb CLI przyjmuje trzy parametry:
\begin{itemize}
  \item \texttt{-{}-input DIR} -- katalog zawierający pliki wideo do~przeanalizowania;
  \item \texttt{-{}-output CSV} -- ścieżka wynikowego pliku CSV z~wynikami detekcji;
  \item \texttt{-{}-mode standard|fast} -- tryb analizy: \texttt{standard} (pełny
    potok z~optical flow, 30~klatek) lub \texttt{fast} (eksperymentalny, ograniczone
    próbkowanie, bez~gwarancji jakości).
\end{itemize}

Jeśli w~katalogu \texttt{-{}-input} umieszczono plik \texttt{labels.csv}
z~kolumnami \texttt{filename} i~\texttt{ground\_truth}, skrypt automatycznie
oblicza recall i~FPR oraz wypisuje podsumowanie po~zakończeniu analizy.
Przykładowe wywołanie:

\begin{verbatim}
python kod/dataset/evaluate.py --input ./videos \
  --output results.csv --mode standard
\end{verbatim}

\noindent\textit{Uwaga:} zapis powyżej ma charakter wielowierszowy; w~środowisku
Windows~11 należy usunąć znaki \texttt{\textbackslash} i~wpisać komendę w~jednej linii.



% -------------------------------------------------------------------
\section{Moduły detekcji}

Warstwa detekcji składa się z~kilku współpracujących modułów
uruchamianych kolejno dla~każdego pliku wideo.

\subsection*{OCR i~analiza nakładek statycznych}

Moduł \texttt{ocr\_detector} odpowiada za~ekstrakcję klatek, normalizację obrazu
i~wykrywanie tekstu metodami OCR. Wykryte regiony tekstowe są filtrowane według
progu pewności i~porównywane ze~słownikiem znanych fraz watermarkingowych
(\enquote{Generated by AI}, \enquote{Runway}, \enquote{Sora} i~inne).
Wyniki są zapisywane w~raporcie wraz ze~współrzędnymi wykrytych obszarów,
co~umożliwia późniejsze przejrzenie klatek z~zaznaczonymi detekcjami.

Analiza statycznych nakładek polega na~obliczeniu mediany temporalnej sekwencji klatek
--- element powtarzający się przez~wiele kolejnych klatek w~tym~samym miejscu
(np.\ logo stacji) daje silny sygnał w~medianie, który odróżnia go
od~treści dynamicznych. Analiza przepływu optycznego pozwala z~kolei wykryć
obszary o~zerowym lub prawie zerowym ruchu, które mogą wskazywać
na~obecność nakładki statycznej.

\subsection*{Analiza częstotliwościowa --- moduł FFT (\texttt{flux\_fft\_detector})}

Moduł \texttt{flux\_fft\_detector} realizuje detekcję opartą na~widmie
częstotliwościowym klatek wideo. Materiały generatywne charakteryzują się
obniżonym udziałem składowych wysokoczęstotliwościowych względem naturalnych
nagrań kamerowych, ponieważ modele dyfuzji i~architektury transformer
generują obrazy o~wygładzonych gradientach i~zredukowanym szumie.
Dla~każdej próbkowanej klatki obliczany jest stosunek energii wysokich
częstotliwości do~całkowitej energii widma (\texttt{freq\_hf\_ratio}),
a~następnie uśredniany po~całej sekwencji.

Wartości \texttt{freq\_hf\_ratio\_mean} istotnie poniżej progu
\texttt{HF\_RATIO\_THRESHOLD} stanowią jeden ze~składowych sygnałów
w~funkcji fuzji. Moduł pełni rolę uzupełniającą wobec analizy przepływu
optycznego --- najsilniej odróżnia materiały Sora i~Runway Gen3 od~nagrań
broadcastowych, dla~których charakterystyczna jest wysoka zmienność
przestrzenna związana z~szumem kompresji i~teksturą tła.

\subsection*{Klasyfikator stylistyczny --- moduł AIStyleCLIP}

Moduł \texttt{ai\_style\_clip\_detector} korzysta z~reprezentacji osadzonych
przez model \texttt{openai/clip-vit-base-patch32} do~oceny, czy sekwencja klatek
przypomina wizualnie materiały generatywne. Na~podstawie 8~równomiernie
próbkowanych klatek budowany jest wektor cech będący konkatenacją średniej
i~odchylenia standardowego embeddingów CLIP (1024~wymiary), a~następnie
używany jest uprzednio wytrenowany klasyfikator binarny
(\texttt{clip\_classifier.pkl}). Model uczono na~zbiorze \texttt{ai\_baseline}
vs.\ \texttt{adv\_fp\_trap} przy użyciu \textit{Logistic Regression}
z~regularyzacją L2 ($C=1{,}0$) oraz 5-krotnej walidacji stratified cross-validation.
Klasyfikator przypisuje każdemu filmowi prawdopodobieństwo przynależności
do~klasy materiałów AI (\texttt{ai\_style\_prob}).

Praca nie raportuje odrębnej, modułowej metryki dla \texttt{AIStyleCLIP},
ponieważ komponent ten oceniano wyłącznie jako część końcowej funkcji fuzji,
a~jego przygotowanie częściowo współdzieli kategorie z~benchmarkiem końcowym.

Wartość ta pełni w~systemie podwójną rolę. Po~pierwsze, wysoki wynik
(\texttt{ai\_style\_prob} $\geq$ \texttt{AI\_STYLE\_CLIP\_HIGH\_CONF\_THRESHOLD})
jest traktowany jako samodzielny sygnał AI-specific, co~może wywołać
detekcję nawet przy~niskim wyniku punktowym funkcji fuzji.
Po~drugie, wartość ta~jest wykorzystywana jako warunek aktywacji
heurystyki \texttt{TC Conditional Boost} opisanej w~sekcji~\ref{sec:tc_boost}
oraz jako warunek blokujący dla~ścieżki kinematycznej opisanej
w~sekcji~\ref{sec:cinematic_guard}.

\subsection*{Spójność temporalna --- moduł TemporalConsistency}

Moduł \texttt{temporal\_consistency\_detector} analizuje trzy sygnały
zmienności w~czasie: wariancję różnic klatek (\texttt{tc\_frame\_diff\_variance}),
gładkość pola przepływu optycznego (\texttt{tc\_of\_smoothness})
oraz odchylenie standardowe luminancji w~czasie (\texttt{tc\_luminance\_temporal\_std}).
Materiały wygenerowane przez modele dyfuzji odznaczają się wyższą
konsekwencją stylistyczną między klatkami niż naturalne nagrania,
które zawierają szum sensora, mikrodrgania i~przypadkowe zmiany oświetlenia.

Wynik modułu (\texttt{tc\_score}) wpływa na~końcowy wynik fuzji
przez mechanizm bonus punktów (\texttt{tc\_bonus}). Sygnał temporalny
jest szczególnie przydatny dla~nagrań po~kompresji H.264,
gdzie artefakty blokowe maskują wysokoczęstotliwościowe cechy widmowe,
a~gładkość ruchu generatywnego pozostaje rozróżnialna.

% -------------------------------------------------------------------
\section{Fuzja sygnałów i~heurystyki decyzyjne}

Wszystkie sygnały produkowane przez poszczególne detektory są agregowane
w~funkcji \texttt{fuse()} zdefiniowanej w~module \texttt{evaluate.py}.
Wynik tej funkcji to~punkt na~skali $[-\infty, \infty]$, przy~czym
próg detekcji (\texttt{POINTS\_THRESHOLD\_DEFAULT}) wynosi~5.

Formalnie funkcję decyzyjną można zapisać jako:
\[
s(x) = \sum_{i=1}^{m} w_i\,h_i(x) - \sum_{j=1}^{n} \lambda_j\,g_j(x) + b(x),
\]
gdzie $h_i(x)$ oznaczają sygnały wzmacniające hipotezę AI,
$g_j(x)$ oznaczają sygnały korygujące charakterystyczne dla materiałów
broadcastowych, a~$b(x)$ obejmuje bonusy warunkowe (np. \texttt{TC Conditional Boost}).
Decyzja klasyfikacyjna ma postać:
\[
\hat{y}(x)=\begin{cases}
1 & \text{gdy } s(x) \geq T,\\
0 & \text{gdy } s(x) < T,
\end{cases}
\quad T=5.
\]
W~praktycznej implementacji współczynniki $w_i$ i~$\lambda_j$ odpowiadają
stałym punktowym przypisanym do~sygnałów na~liście poniżej
(np. $w_i \in \{1,2,3\}$ dla sygnałów wzmacniających oraz
$\lambda_j \in \{2,3\}$ dla korekt broadcastowych).

Poszczególne sygnały wnoszą następujące wartości punktowe:
obecność przepływu optycznego i~niski stosunek obszaru nakładki~(+1~każdy),
niska wariancja tekstury i~niski udział HF przy~kształcie AI~(+1~każdy),
regiony zero-variance~(+1), narożna, kompaktowa nakładka~(+1),
niewidzialny watermark~(+2), metadane C2PA~AI~(+2), sygnał modułu Flux
(\texttt{flux\_fft\_detector})~(+3).
Korekty negatywne eliminują wzorce charakterystyczne dla~transmisji sportowych
i~pasków informacyjnych: szeroki dolny overlay~($-2$), heurystyki
scoreboard/billboard/lower-third~(od~$-2$ do~$-3$).

\subsection*{Heurystyki zabezpieczające}

Oprócz podstawowej funkcji punktowej w~systemie działają trzy
dodatkowe mechanizmy zabezpieczające przed~fałszywymi alarmami:

\begin{itemize}
  \item \textbf{\texttt{broadcast\_score\_cap}} --- ogranicza wynik do~3~punktów
    dla~filmów, które uaktywniły pułapkę wzorca broadcastowego,
    a~nie mają potwierdzenia AI z~C2PA ani Flux;
  \item \textbf{\texttt{of\_penalty}} --- odejmuje~2~punkty, gdy liczba regionów
    przepływu optycznego przekracza~20 przy~braku sygnału AI-specific,
    co~chroni przed~detekcją dynamicznych nagrań sportowych i~koncertowych;
  \item \textbf{\texttt{high\_score\_override\_guard}} --- wynik punktowy powyżej progu
    \texttt{HIGH\_SCORE\_OVERRIDE\_THRESHOLD} (domyślnie~6) nie wywołuje
    detekcji, jeżeli \texttt{ai\_specific}~$= 0$; chroni to~przed sytuacją,
    w~której duże bonusy FFT na~materiale broadcastowym sztucznie zawyżają
    wynik bez~potwierdzenia ze~strony sygnałów specyficznych dla~AI;
  \item \textbf{\texttt{clean\_ai\_rescue}} --- specjalna ścieżka detekcji dla~filmów
    bez~widocznych nakładek, z~niskim udziałem HF i~bez~jakichkolwiek
    sygnałów broadcastowych, dedykowana materiałom Sora i~Runway Gen3,
    które nie zawierają watermarków tekstowych.
\end{itemize}

% -------------------------------------------------------------------
\section{\texttt{TC Conditional Boost}}
\label{sec:tc_boost}

Jedną z~heurystyk zastosowanych w~końcowej wersji detektora jest mechanizm
\texttt{TC Conditional Boost} (\textit{Temporal Consistency Boost}). Jest to~reguła
późnej fuzji działająca na~przypadkach granicznych --- filmach, których
wynik po~zsumowaniu wszystkich sygnałów mieści się w~przedziale $s \in \{2, 3\}$
punktów, co~bez~dodatkowej interwencji skutkowałoby klasyfikacją FN.
Formalnie jest to heurystyka późnej fuzji oparta na warunkowym
wzmocnieniu wyniku klasyfikacji o~$+1$ punkt przy jednoczesnym spełnieniu
warunków temporalnych i~stylometrycznych.

Mechanizm zwiększa wynik o~jeden punkt, gdy spełnione są jednocześnie
trzy warunki:
\begin{enumerate}
  \item detektor spójności temporalnej aktywował się ($\texttt{tc\_detected} = 1$),
  \item ocena stylistyczna CLIP mieści się w~przedziale umiarkowanego
    sygnału AI: $p_\text{AI} \geq \texttt{TC\_MIN\_AI\_STYLE\_PROB} = 0{,}44$,
  \item wartość $p_\text{AI}$ jest jednocześnie poniżej progu
    $\texttt{TC\_MAX\_LOWER\_THIRD\_PROB} = 0{,}50$,
    co~eliminuje materiały broadcastowe z~animowanymi planszami dolnej trzeciej,
    u~których CLIP może generować umiarkowany wynik AI.
\end{enumerate}

Warunek~(3) jest kluczowy z~punktu widzenia precyzji. Materiały z~kategorii
\texttt{adv\_fp\_trap} zawierające pełnoekranowe plansze lub paski informacyjne
charakteryzują się zazwyczaj $p_\text{AI} \geq 0{,}50$ lub aktywnym sygnałem
\texttt{broadcast\_trap}, co~blokuje aktywację boostu. W~zbiorze ewaluacyjnym
żadna z~próbek broadcastowych nie spełniła wszystkich trzech warunków
jednocześnie (\texttt{tc\_boost\_applied}~$= 0$ we~wszystkich przypadkach
\texttt{adv\_fp\_trap}).

Boost jest zaimplementowany jako wydzielona funkcja \texttt{tc\_boost()}
w~\texttt{evaluate.py}, korzystająca wyłącznie ze~stałych zdefiniowanych
w~\texttt{fusion\_params.py}. Dzięki temu parametry heurystyki można
modyfikować bez~ingerencji w~logikę pozostałych modułów.

\section{\texttt{Cinematic Path Guard}}
\label{sec:cinematic_guard}

Dodatkowe zabezpieczenie systemu opiera się na~obserwacji, że
ścieżka kinematyczna (wysoka liczba wektorów przepływu optycznego
i~niski udział składowych HF) może aktywować detekcję AI
na~naturalnych nagraniach z~dużym ruchem --- nocnych scenach miejskich,
efektach graficznych renderowanych w~czasie rzeczywistym czy dynamicznie
filmowanych wydarzeniach sportowych.

Mechanizm \texttt{Cinematic Path Guard} (heurystyka \texttt{cinematic\_rescue\_guard})
działa dwutorowo. Po~pierwsze,
gdy sygnał AI-specific pochodzi wyłącznie ze~ścieżki kinematycznej
(warunki: $\texttt{of\_count} \geq 3$, $\texttt{freq\_hf\_ratio} < 0{,}52$,
$\texttt{of\_wide\_lower} = 0$), a~klasyfikator CLIP przypisuje
prawdopodobieństwo poniżej $\texttt{CINEMATIC\_CLIP\_MIN\_PROB} = 0{,}20$,
flaga \texttt{ai\_specific} jest zerowana, co~blokuje detekcję dla~materiałów
bez~rzeczywistych cech generatywnych. Po~drugie, ścieżka
\texttt{clean\_ai\_rescue\_motion} --- dedykowana filmom bez~nakładek
o~bogatym ruchu kamerowym (Runway Gen3, Luma Modify) --- wymaga spełnienia
co~najmniej jednego z~warunków: $p_\text{AI} \geq \texttt{CINEMATIC\_RESCUE\_MIN\_CLIP\_PROB} = 0{,}20$
lub obecności kompaktowej narożnej nakładki (\texttt{of\_corner\_compact\_roi\_count}~$\geq 1$).
Formalnie jest to heurystyka ochronna ograniczająca fałszywe alarmy,
która ogranicza aktywację ścieżki kinematycznej bez~silnego potwierdzenia
sygnałami specyficznymi dla~AI.
Dzięki temu nagrania w~rodzaju \enquote{Ginza in Tokyo} czy teledyski
z~agresywną dynamiką kamery nie są błędnie klasyfikowane jako materiał AI.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.95\textwidth]{../photos/analized_frames.png}
  \caption{Widok raportu wynikowego wygenerowanego przez aplikację dla jednego pliku wideo.
    Prezentuje wybrany fragment tabeli wyników z najważniejszymi kolumnami sygnałów i metadanych.
    Taki raport wspiera weryfikację decyzji klasyfikacyjnej na poziomie konkretnych atrybutów.}
  \label{fig:frames}
\end{figure}

\begin{figure}[htbp]
  \centering
  \includegraphics[width=0.85\textwidth]{../photos/analized_frames_folder.png}
  \caption{Struktura katalogu wynikowego wygenerowanego przez aplikację
    dla~jednego pliku wideo. Katalog zawiera: zapisane klatki z~zaznaczonymi
    wykryciami (\texttt{frame\_*.jpg}), obraz mediany temporalnej
    (\texttt{median\_frame.jpg}), wizualizację analizy FFT
    (\texttt{fft\_spectrum.jpg}) oraz raport \texttt{report.csv}
    z~pełnym zestawem metadanych detekcji.}
  \label{fig:folder}
\end{figure}

Rysunki~\ref{fig:frames} i~\ref{fig:folder} pokazują, że system pozostawia
ślad audytowy na poziomie klatek, cech i~raportów pomocniczych. Nie eliminuje to
w~pełni problemu \enquote{czarnej skrzynki}, ale wyraźnie ułatwia weryfikację
decyzji klasyfikacyjnej i~analizę błędów.

% -------------------------------------------------------------------
\section{Ewaluacja ilościowa}
\label{sec:ewaluacja}

Ewaluacja ilościowa opisana w~niniejszym rozdziale była przeprowadzana
przy użyciu skryptu \texttt{evaluate.py} uruchamianego z~wiersza poleceń,
co~zapewnia reprodukowalność procedury benchmarkowej bez~interakcji
z~interfejsem graficznym.
GUI stanowi uzupełniające narzędzie operacyjne przeznaczone do~analizy
pojedynczych plików lub małych partii materiałów.
Skrypt obsługuje trzy kluczowe argumenty:
\texttt{{-}{-}input} (katalog z~plikami wideo),
\texttt{{-}{-}mode} (\texttt{standard}~/~\texttt{fast})
oraz \texttt{{-}{-}output} (ścieżka do~wynikowego pliku CSV).
Przyłączony do~katalogu plik \texttt{labels.csv}
umozliwia automatyczne obliczenie recall i~FPR po~zakończeniu analizy.

Ewaluację przeprowadzono na~zbiorze 137~plików wideo podzielonych
na~cztery kategorie testowe (tabela~\ref{tab:eval_splits}).
Zastosowano metryki klasyfikacji binarnej: recall (TPR),
wskaźnik fałszywych alarmów (FPR), precyzję oraz F1.

\begin{table}[h]
\centering
\caption{Skład zbioru ewaluacyjnego według kategorii}
\label{tab:eval_splits}
\begin{tabular}{llr}
\toprule
Kategoria & Opis & $N$ \\
\midrule
\texttt{ai\_baseline}    & Filmy syntetyczne bez modyfikacji              & 43 \\
\texttt{adv\_compressed} & Filmy AI po kompresji H.264                    & 26 \\
\texttt{adv\_cropped}    & Filmy AI po przycięciu kadrów                 & 26 \\
\texttt{adv\_fp\_trap}   & Materiały broadcastowe (klasa negatywna)       & 42 \\
\bottomrule
\end{tabular}
\end{table}

Split \texttt{ai\_baseline} zawiera 43~unikalne filmy źródłowe wygenerowane
przez różne modele AI. Splity \texttt{adv\_compressed} oraz
\texttt{adv\_cropped} nie reprezentują nowych treści semantycznych, lecz
transformacyjne warianty 26~z~tych 43~filmów. Oznacza to, że 95~rekordów dodatnich
nie odpowiada 95~unikalnym scenom, lecz 43~źródłowym scenom oraz 52~wariantom
adwersarialnym. Klasa negatywna \texttt{adv\_fp\_trap} obejmuje 42~autentyczne
materiały broadcastowe z~nakładkami graficznymi. Pełne manifesty plików
utrzymywane są w~repozytorium projektu.

Etykiety przypisano ręcznie na podstawie źródła pliku, deklarowanego generatora
oraz inspekcji materiału. W~trybie \texttt{standard} skrypt \texttt{evaluate.py}
próbkuje 30~klatek rozmieszczonych równomiernie w~całej długości filmu, dzięki
czemu wyniki nie zależą od ręcznego wyboru pojedynczego fragmentu.

\subsection*{Wyniki końcowe}

Tabela~\ref{tab:eval_results} przedstawia wyniki końcowej wersji detektora
po~wprowadzeniu poprawek \texttt{high\_score\_override\_guard}
i~\texttt{cinematic\_rescue\_guard}. Kategoria \texttt{adv\_fp\_trap}
jest zbiorem wyłącznie negatywnym, dlatego recall nie jest dla niej wyznaczany.
Wartość F1 raportowana jest wyłącznie globalnie, ponieważ poszczególne splity
dodatnie nie mają własnych liczników FP.

\begin{table}[h]
\centering
\caption{Metryki klasyfikacji końcowej wersji detektora}
\label{tab:eval_results}
\begin{tabular}{lrrrrr}
\toprule
Kategoria & $N$ & TP & FN & Recall & FPR \\
\midrule
\texttt{ai\_baseline}    & 43  & 26 & 17 & 0{,}605 & --- \\
\texttt{adv\_compressed} & 26  & 15 & 11 & 0{,}577 & --- \\
\texttt{adv\_cropped}    & 26  & 14 & 12 & 0{,}538 & --- \\
\texttt{adv\_fp\_trap}   & 42  & --- & --- & ---     & 0{,}095 \\
\textbf{Globalnie}       & 137 & 55 & 40 & 0{,}579 & 0{,}095 \\
\bottomrule
\end{tabular}
\end{table}

\noindent\textit{Nota: Dla kategorii \texttt{adv\_fp\_trap} (klasa negatywna)
recall nie jest zdefiniowany, dlatego podano wyłącznie FPR. Standardowe F1
nie jest raportowane per split, ponieważ wymaga wspólnej przestrzeni TP/FP/FN;
w~tej pracy sens ma tylko F1 globalne liczone na~pełnym zbiorze ewaluacyjnym.
Dla wszystkich 137~próbek globalna precyzja wynosi 0{,}932, a~globalne
F1 wynosi 0{,}714.}

Detektor osiągnął recall~0{,}605 na~zbiorze bazowym
i~0{,}577--0{,}538 na~zbiorach z~modyfikacją kompresji i~przycięcia.
Wskaźnik fałszywych alarmów wyniósł $\mathrm{FPR} = 0{,}095$
(4~FP na~42~próbkach negatywnych) dla~kategorii \texttt{adv\_fp\_trap},
co~plasuje się wyraźnie poniżej wyznaczonego progu $1/7 \approx 0{,}143$.
Próg ten nie pochodzi z~literatury jako uniwersalny standard, lecz został
przyjęty pragmatycznie jako punkt pracy dla pilotażowego scenariusza newsroomowego:
średnio nie więcej niż jeden fałszywy alarm na~siedem trudnych materiałów
broadcastowych kierowanych do~ręcznej weryfikacji.
W~porównaniu z~wcześniejszą wersją detektora ($\mathrm{FPR} = 0{,}190$, 8~FP)
wprowadzone poprawki wyeliminowały połowę fałszywych alarmów
bez~utraty żadnego prawdziwego pozytywu.
Precyzja globalna wynosi $55/(55+4) \approx 0{,}932$ na~zbiorze ewaluacyjnym,
przy~czym należy zaznaczyć, że~zbiór ten~nie jest w~pełni zrównoważony
--- 95~próbek pozytywnych wobec~42~negatywnych --- co~zawyża bezwzględną
wartość precyzji i~sprawia, że~recall oraz FPR są bardziej miarodajnymi
wskaźnikami jakości detektora w~tym~kontekście. Przybliżone 95\% przedziały
ufności Wilsona dla pełnego zbioru wynoszą odpowiednio:
recall~$\in [0{,}478;\,0{,}673]$ oraz FPR~$\in [0{,}038;\,0{,}221]$.
Wyniki te należy więc interpretować jako pilotażowe oszacowania, a~nie
stabilne parametry populacyjne. Przy zachowaniu $\text{FPR} = 0{,}095$
żadna konfiguracja z badanego sweepu bazowej funkcji fuzji nie osiągała
recall powyżej $0{,}605$ --- oznacza to, że dalsze zwiększenie czułości
detekcji wymagałoby akceptacji wyższego wskaźnika fałszywych alarmów,
rozbudowy danych lub zmiany samej architektury potoku.

\subsection*{Analiza przestrzeni parametrów --- front Pareto}

Dobór domyślnego progu detekcji nie był arbitralny. Przeprowadzono
sweep parametrów bazowej funkcji fuzji, generując kilkadziesiąt
konfiguracji różniących się wartością progu punktowego, wagami
poszczególnych sygnałów i~aktywnością heurystyk zabezpieczających.
Każda konfiguracja została oceniona parą metryk: TPR na~zbiorze
\texttt{ai\_baseline} oraz FPR na~zbiorze \texttt{adv\_fp\_trap}.
Przetestowano 54 konfiguracje parametrów fuzji, spośród których na front Pareto
weszła jedna unikalna konfiguracja w~obrębie tego konkretnego sweepu.

Rysunek~\ref{fig:pareto} przedstawia front Pareto wyznaczony z~wyników
tego sweepowania. Każdy szary punkt odpowiada jednej testowanej konfiguracji;
niebieski punkt wyznacza jedyną konfigurację niezdominowaną
spośród 54 badanych ustawień. Czerwona gwiazdka oznacza punkt operacyjny
wybranej konfiguracji końcowej: $\mathrm{FPR} = 0{,}095$, $\mathrm{TPR} = 0{,}60$.
Nie jest to ten sam punkt co~front Pareto z~rysunku: konfiguracja końcowa
powstała po~późniejszych korektach heurystyk ochronnych, dlatego wykres należy
traktować jako mapę przestrzeni parametrów bazowej funkcji fuzji, a~nie jako
dowód, że finalny punkt pracy leży na tym froncie.
Pomarańczowa linia przerywana wyznacza cel projektowy
$\mathrm{FPR} \leq 1/7 \approx 0{,}143$; zielona strefa pożądana
zaznacza obszar, w~którym FPR i~TPR spełniają jednocześnie oba kryteria.

\begin{figure}[H]
  \centering
  \includegraphics[width=0.88\textwidth]{figures/07_pareto_frontier.png}
  \caption{Mapa przestrzeni parametrów bazowej funkcji fuzji w~układzie FPR/TPR.
    Szare punkty to~wszystkie testowane konfiguracje sweepu; niebieski punkt
    oznacza jedyną konfigurację niezdominowaną w~tym sweepie.
    Czerwona gwiazdka ($\mathrm{FPR}=0{,}095$, $\mathrm{TPR}=0{,}60$)
    odpowiada konfiguracji końcowej po~późniejszych poprawkach heurystycznych,
    dlatego nie należy utożsamiać jej z~punktem frontu Pareto.
    Pomarańczowa linia przerywana wyznacza cel projektowy
    $\mathrm{FPR} \leq 1/7 \approx 0{,}143$. Zielona strefa oznacza obszar
    pożądany z~punktu widzenia przyjętego punktu pracy.}
  \label{fig:pareto}
\end{figure}

Istotna obserwacja wynikająca z~wykresu: testowane konfiguracje skupiały się
w wąskim przedziale $\mathrm{FPR} \in [0{,}05;\, 0{,}12]$, natomiast front
Pareto zredukował się do jednej unikalnej konfiguracji. Oznacza to, że w badanym
sweepie nie znaleziono alternatywnej konfiguracji, która poprawiałaby TPR
bez pogorszenia FPR w~obrębie bazowego sweepu. Konfiguracja końcowa została
następnie dobrana osobno jako konserwatywny punkt operacyjny po~dodaniu
mechanizmów ochronnych spoza samego sweepu.

\subsection*{Analiza błędów}

Fałszywe negatywy w~zbiorze \texttt{ai\_baseline} (17~przypadków na~43)
dotyczą materiałów generatywnych, w~których generator nie osadził
widocznego watermarku, a~sygnały pośrednie (HF, texture, TC) nie
przekraczają progów przy~braku sygnału Flux lub C2PA.
Są to~głównie krótkie klipy z~dużym ruchem kamerowym i~złożoną
scenografią --- ich charakterystyka wizualna zbliża się do~naturalnych
nagrań dokumentalnych.

Cztery pozostałe fałszywe pozytywy w~zbiorze \texttt{adv\_fp\_trap}
to~materiały z~aktywnym \texttt{tc\_detected} i~umiarkowanym wynikiem
przepływu optycznego, ale bez~sygnałów broadcast ani sygnału CLIP.
Obejmują nagrania z~dynamiczną grafiką (efekty AR w~transmisjach sportowych,
interaktywne plansze studyjne) --- ich profil widmowy i~teksturowy
jest na~tyle zbliżony do~materiałów generatywnych,
że~żadna z~istniejących heurystyk nie jest w~stanie ich odróżnić
bez~ryzyka pominięcia rzeczywistych materiałów AI o~podobnym charakterze.
Ponieważ \texttt{TC Conditional Boost} nie aktywuje się dla~$s \geq 4$
(warunek \texttt{TC\_ELIGIBLE\_SCORES}), żaden z~tych przypadków
nie został wzmocniony przez tę heurystykę ---
błędna klasyfikacja pochodzi wyłącznie z~bazowej funkcji fuzji.

% -------------------------------------------------------------------
\subsection*{Aktywacje heurystyk}

Tabela~\ref{tab:heuristics} sumaryzuje aktywacje kluczowych komponentów systemu
na zbiorze ewaluacyjnym (N\,=\,137):

\begin{table}[h]
\centering
\caption{Aktywacje heurystyk systemu detekcji na zbiorze ewaluacyjnym}
\label{tab:heuristics}
\begin{tabular}{lrrl}
\toprule
Heurystyka & Aktywacje & w tym na TP & Główne zastosowanie \\
\midrule
OCR watermark & 38 & 36 & ai\_baseline z~widocznym znakiem \\
Moduł FFT (\texttt{flux\_fft\_detector}) & 31 & 31 & wideo Sora / Runway Gen-3 \\
\texttt{AIStyleCLIP} & 24 & 22 & uzupełniający sygnał semantyczny \\
\texttt{TemporalConsistency} & 47 & 43 & detekcja anomalii ruchu \\
\texttt{TC Conditional Boost} & 12 & 11 & przypadki graniczne, wzmacnianie \\
\texttt{clean\_ai\_rescue} & 8 & 8 & klipy bez~widocznych nakładek \\
\texttt{broadcast\_score\_cap} & 19 & 0 & ograniczenie FP na~materiałach telewizyjnych \\
\texttt{of\_penalty} & 9 & 0 & penalizacja wysokiego optycznego ruchu \\
C2PA & 14 & 14 & metadane proweniencji \\
\bottomrule
\end{tabular}
\end{table}

Warto zauważyć, że moduł FFT i~\texttt{C2PA} aktywowały się wyłącznie
na trafieniach prawdziwie dodatnich, co wskazuje na ich wysoką selektywność.
Z kolei \texttt{broadcast\_score\_cap} oraz \texttt{of\_penalty} pełniły przede wszystkim rolę ochronną,
ograniczając liczbę fałszywych alarmów na materiałach telewizyjnych.
Heurystyka \texttt{clean\_ai\_rescue} aktywowała się rzadko, lecz wyłącznie na materiałach dodatnich,
co potwierdza jej użyteczność jako ścieżki ratunkowej dla „czystych” materiałów AI.

% -------------------------------------------------------------------
\subsection*{Globalna macierz pomyłek}

\begin{table}[h]
\centering
\caption{Globalna macierz pomyłek detektora (zbiór ewaluacyjny, N\,=\,137)}
\label{tab:confusion_matrix}
\begin{tabular}{lcc}
\toprule
 & \textbf{Prognoza: AI} & \textbf{Prognoza: nie-AI} \\
\midrule
\textbf{Faktycznie: AI}     & TP = 55 & FN = 40 \\
\textbf{Faktycznie: nie-AI} & FP = 4  & TN = 38 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.55\textwidth]{figures/confusion_matrix.pdf}
  \caption{Graficzna reprezentacja globalnej macierzy pomyłek detektora (zbiór ewaluacyjny, N\,=\,137). Liczby w komórkach pozostają głównym nośnikiem informacji, a poziome opisy osi poprawiają czytelność.}
  \label{fig:confusion_matrix_heatmap}
\end{figure}

\noindent Tabela ujawnia, że główną przeszkodą w~poprawie skuteczności
jest niska liczba prawdziwych pozytywów względem całej populacji AI
(55 na~95, tj.\ recall 0,579),
natomiast wskaźnik fałszywych alarmów jest dobrze kontrolowany (4 na~42, tj.\ FPR 0,095).

Przy N\,=\,42 próbkach negatywnych zmiana o~jeden FP przek\l{}ada si\k{e} na~\textasciitilde{}2,4\,pp ---
wyniki nale\.{z}y traktowa\'{c} jako orientacyjne.

Ze wzgl\k{e}du na umiarkowan\k{a} liczebno\'{s}\'{c} pr\'{o}b w poszczeg\'{o}lnych splitach
uzyskane warto\'{s}ci Recall i FPR nale\.{z}y interpretowa\'{c} jako oszacowania obarczone
niepewności\k{a}, a nie jako warto\'{s}ci stabilne dla wszystkich przysz\l{}ych zbior\'{o}w danych.

Z punktu widzenia praktycznego oznacza to, że obecna konfiguracja systemu
jest konserwatywna: znacznie częściej pomija część materiałów AI
niż generuje fałszywe alarmy na materiałach autentycznych.
Należy jednak pamiętać, że ze względu na niewielki rozmiar zbioru danych (N\,=\,137),
przedziały ufności dla tych wskaźników są szerokie, co czyni wyniki charakterem
pilotażowym i wymagającym dalszej walidacji na większych zbiorach.

% -------------------------------------------------------------------
\section{Ograniczenia metody i~zagrożenia poprawności}
\label{sec:threats_to_validity}

Przeprowadzona ewaluacja podlegała szeregowi ograniczeń, które znajdują się w~bezpośredniej relacji
do~ogólności uzyskanych wyników:

Najistotniejszym ograniczeniem metodologicznym pozostaje częściowo wewnętrzny
charakter przygotowania modułu \texttt{AIStyleCLIP}; pozostałe ograniczenia
należy interpretować na~tym tle.

\begin{enumerate}

  \item \textbf{Zbiór danych i~jego niezrównoważenie.}
    Zbiór ewaluacyjny zawiera 95~próbek pozytywnych (wideo AI) i~42~próbki negatywne (broadcast).
    Nierównowaga ta zawyża znaczenie precyzji i~sprawia, że~recall i~FPR są bardziej miarodajne.
    Ponadto negatywy pochodzą wyłącznie z~jednego źródła (broadcast), co~ogranicza generalność
    oceny w~stosunku do~innych rodzajów naturalnych zawartości (np.\ dokumentacja, edukacja).

  \item \textbf{Transfer gap między modelami generatywnymi.}
    Część danych pozytywnych pochodzi z~modeli obecnych w~momencie ewaluacji (Runway, Sora).
    Nowsze modele generatywne mogą produkować artefakty o~innym charakterze, na~które
    detektor nie został w~pełni trenowany lub dostrojony. Doświadczenia z~literatury
    (sekcja~\ref{sec:otwarte_problemy}) pokazują spadek AUC o~10--30 punktów procentowych
    przy testowaniu na~nowych generatorach.

  \item \textbf{Tuning heurystyk na~własnym benchmarku.}
    Wiele heurystyk, takich jak \texttt{TC Conditional Boost}, \texttt{Cinematic Path Guard}
    i~broadcast guard, dostrajano bezpośrednio na~własnym zbiorze ewaluacyjnym.
    Stwarza to~ryzyko częściowego przeuczenia do~charakterystyki benchmarku,
    ograniczone poprzez sweep parametrów bazowej funkcji fuzji i~wybór
    konserwatywnego punktu pracy odniesionego do~analizy Pareto
    (sekcja~\ref{sec:ewaluacja}).
    Ideałem byłby podział na~zbiór treningowy, walidacyjny i~testowy oraz
    wczesne zatrzymanie na~podstawie metryki walidacyjnej bez~dostępu do~zbioru testowego.

  \item \textbf{Częściowo wewnętrzny charakter ewaluacji modułu \texttt{AIStyleCLIP}.}
    Moduł \texttt{AIStyleCLIP} przygotowano na~podstawie materiałów z~kategorii
    \texttt{ai\_baseline} i~\texttt{adv\_fp\_trap}, czyli z~tej samej rodziny kategorii,
    która została następnie wykorzystana w~ewaluacji końcowej całego potoku.
    Oznacza to, że benchmark nie jest w~pełni niezależny względem wszystkich
    komponentów systemu i~dla tego modułu ma częściowo charakter walidacji wewnętrznej.
    W~repozytorium nie utrzymywano odrębnego, rozłącznego zbioru treningowego
    przeznaczonego wyłącznie dla tego klasyfikatora, dlatego pełna ocena jego
    generalizacji wymagałaby osobnej kampanii na~zewnętrznym zestawie danych.

  \item \textbf{Brak szerokiej walidacji na~realnych danych.}
    Materiały \texttt{realclean} (tj.\ autentyczne nagrania pobrane z~Internetu, niepochodzące z~broadcastu i~nieposiadające graficznych nakładek)
    nie były w~pełni wykorzystane w~ewaluacji ilościowej, tylko w~analizie jakościowej.
    Rzeczywista wydajność w~produkcji może różnić się od~wyników na~zbiorze
    eksperymentalnym, szczególnie w~obecności nowych stylów kamer i~edycji.

  \item \textbf{Porównania komercyjnych narzędzi na~różnych próbach.}
    Każde narzędzie komercyjne było testowane na~innym podzbiorze ze względów logistycznych
    (limit zapytań, koszt). To uniemożliwia bezpośrednie benchmarkowe porównanie 1:1
    i~powoduje, że wyniki w~sekcji~\ref{sec:porownanie_zbiorcze}
    mają charakter orientacyjny, a~nie porównawczy.

  \item \textbf{Ograniczona interpretacja sygnałów}. 
    Każdy z~modułów detekcji (FFT, CLIP, TC, C2PA) dostarcza wyłącznie wyniku liczbowego.
    Brak pogłębionych analiz błędów II rodzaju (czy detektor zawsze popełnia te same błędy?)
    lub pewnych kategorii wizualnych (czy pewne profile wideo są systematycznie pominięte).
    Dostępne artefakty diagnostyczne częściowo ograniczają ten problem, ale nie zastępują
    pełnej interpretowalności modelowej.

\end{enumerate}

\begin{table}[h]
\centering
\caption{Threats to Validity --- podsumowanie}
\label{tab:threats_summary}
\small
\begin{tabular}{L{4.6cm}L{4.5cm}L{5.8cm}}
\toprule
\textbf{Zagrożenie} & \textbf{Wpływ} & \textbf{Sposób ograniczenia} \\
\midrule
Niezrównoważony zbiór i~wąski profil negatywów &
Możliwe zawyżenie stabilności metryk poza broadcastem &
Wykorzystanie FPR i~Recall jako metryk głównych; plan rozszerzenia danych \texttt{realclean} \\
\addlinespace[2pt]
Transfer gap między generatorami &
Spadek skuteczności na~nowszych modelach &
Modułowa fuzja sygnałów i~aktualizacja heurystyk bez~przebudowy potoku \\
\addlinespace[2pt]
Tuning heurystyk na~tym samym benchmarku &
Ryzyko częściowego przeuczenia &
Sweep parametrów, wybór punktu Pareto; docelowo osobny split walidacyjny \\
\addlinespace[2pt]
Częściowo wewnętrzny charakter \texttt{AIStyleCLIP} &
Ryzyko zawyżenia oceny tego komponentu względem danych końcowych &
Docelowo rozłączny train/validation/test lub zewnętrzny zbiór dla modułu CLIP \\
\addlinespace[2pt]
Brak szerokiej walidacji na~realnych danych &
Niepewność co do zachowania poza materiałami broadcastowymi &
Rozszerzenie negatywów o~zbiór \texttt{realclean} i~zewnętrzny test set \\
\addlinespace[2pt]
Porównania narzędzi na~różnych próbach &
Brak pełnej porównywalności 1:1 &
Wyraźne oddzielenie tabel porównania wspólnego ($N=79$) i~poglądowego \\
\addlinespace[2pt]
Ograniczona interpretacja sygnałów &
Trudność w~wyjaśnieniu wszystkich FN/FP na poziomie przesłanek decyzji &
Zapis artefaktów diagnostycznych i~ręczna analiza błędów, lecz bez pełnej interpretowalności modelowej \\
\bottomrule
\end{tabular}
\end{table}

Pomimo tych ograniczeń wyniki wspierają ostrożny wniosek, że podejście
wielosygnałowe może pełnić rolę narzędzia wspomagającego triage,
ale nie uzasadniają jeszcze tez o~szerokiej gotowości produkcyjnej
ani pełnej walidacji zewnętrznej wszystkich komponentów.

\subsection*{Eksploracyjna ocena wkładu komponentów}
W tej wersji pracy nie przeprowadzono pełnej, odrębnej kampanii ablacyjnej
z~ponownym uruchomieniem benchmarku dla każdej konfiguracji. Zamiast tego
przedstawiono \textbf{eksploracyjną estymację} wkładu wybranych komponentów,
opartą na logach aktywacji heurystyk, pomiarach czasu i~kontrolnych wyłączeniach
najważniejszych modułów. Tabeli~\ref{tab:ablation} nie należy zatem interpretować
jako pełnoprawnej analizy ablacyjnej w~sensie eksperymentalnym, lecz jako
pomocniczy opis względnego znaczenia poszczególnych warstw potoku.
\begin{table}[h]
\centering
\caption{Eksploracyjna estymacja wpływu wyłączenia modułów na recall i FPR}
\label{tab:ablation}
\begin{tabular}{lccc}
\hline
\textbf{Konfiguracja} & \textbf{Recall (ai\_baseline)} & \textbf{FPR (fp\_trap)} & \textbf{Czas/plik} \\
\hline
Pełny system (standard) & 0{,}605 & 0{,}095 & $\approx$11{,}4\,s \\
Bez bonusu FFT & 0{,}591 & 0{,}095 & $\approx$10{,}9\,s \\
Bez modułu CLIP (\texttt{ai\_style\_prob}=0) & 0{,}570 & 0{,}095 & $\approx$9{,}6\,s \\
Bez OF, zredukowane próbkowanie & 0{,}326 & 0{,}381 & $\approx$0{,}8\,s \\
\hline
\end{tabular}
\end{table}
Estymacja wskazuje na~istotną hierarchię sygnałów: wyłączenie FFT i~CLIP obniża recall
o~2--4~pp przy~niezmiennym FPR, natomiast wyłączenie optical flow powoduje
ponad czterokrotny wzrost fałszywych alarmów. Guardy heurystyczne oparte na~warunkach
\texttt{of\_count~$\leq$~8} i~statycznej ścieżce ratunkowej dla części klipów Sora trywialnie aktywowały
się przy~wyłączonym detektorze przepływu, co~potwierdza, że ich projektowanie zakłada
obecność sygnału OF jako kotwicy decyzyjnej.
Opis ten uzasadnia pozostawienie pełnego potoku jako konfiguracji domyślnej,
ale nie zastępuje przyszłej, w~pełni zmierzonej kampanii ablacyjnej.

\noindent\textit{Uwaga:} czasy z~tabeli~\ref{tab:ablation} pochodzą z~oddzielnych,
eksploracyjnych pomiarów wariantów potoku i~mają charakter względny; nie należy ich
porównywać wprost ze średnim czasem z~wyjścia CLI, które odnosi się do osobnego,
pełnego przebiegu benchmarku na zbiorze 137~plików.

% -------------------------------------------------------------------
\section{Reprodukowalność procedury eksperymentalnej}
\label{sec:reprodukowalnosc}

Ewaluację przeprowadzono w~środowisku \textbf{Python~3.11} na~systemie \textbf{Windows~11}.
Kluczowe biblioteki:
\begin{itemize}
  \item \textbf{OpenCV}~4.9 (przetwarzanie wideo i~analizy FFT)
  \item \textbf{PyQt6}~6.7 (interfejs użytkownika)
  \item \textbf{EasyOCR}~1.7 (backend OCR użyty w~benchmarku referencyjnym; RapidOCR/PaddleOCR pozostają opcjonalnymi backendami)
  \item \textbf{NumPy}~1.26 oraz \textbf{SciPy}~1.13 (operacje numeryczne i~Signal Processing)
  \item \textbf{Pandas}~2.2 (zarządzanie danymi i~raporty)
  \item \textbf{CLIP}~\texttt{openai/clip-vit-base-patch32} (detekcja stylu AI)
  \item \textbf{scikit-learn}~1.5 (metryki i~ewaluacja)
\end{itemize}

Kod źródłowy systemu dostępny jest w~repozytorium projektu:
\url{https://github.com/bartpomietlo/magisterka}.
Implementacja opierała się na~dokumentacjach technicznych bibliotek
\textbf{OpenCV}, \textbf{PyQt6}, \textbf{EasyOCR} i~specyfikacji
\textbf{C2PA}, natomiast warstwę koncepcyjną wspierały publikacje
omówione w~rozdziałach~2--3.
Wersji benchmarkowej odpowiada commit \texttt{196bfa7}
(pełny skrót: \texttt{196bfa74ea54000d5da382187379572ef5bee686}),
na~którym zamrożono wyniki przywoływane w~niniejszym rozdziale.
Benchmark uruchamia się komendą:
\begin{verbatim}
python kod/dataset/evaluate.py --mode standard \
  --input ./test_dataset --output results/out.csv
\end{verbatim}

Dodając do~katalogu plik \texttt{labels.csv} z~kolumnami
\texttt{filename} i~\texttt{ground\_truth}, skrypt automatycznie
oblicza recall i~FPR po~zakończeniu i~wypisuje podsumowanie. Poniższe wyjście CLI
dotyczy pełnego zbioru 137~próbek, a~więc recall 0{,}579 odnosi się do wszystkich
95~próbek dodatnich łącznie; dla samej kategorii \texttt{ai\_baseline}
wartość wynosi 0{,}605 (tabela~\ref{tab:eval_results}):
\begin{verbatim}
[SUMMARY] Pliki: 137  Przeanalizowane: 137
[SUMMARY] Czas analizy: sredni=4.21s  min=1.83s  max=11.04s
[SUMMARY] TP=55 FN=40 FP=4 TN=38
[SUMMARY] Recall=0.579  FPR=0.095
\end{verbatim}

Model \texttt{clip\_classifier.pkl} przygotowano skryptem
\texttt{kod/tools/train\_clip\_classifier.py} na podstawie 8~równomiernie
próbkowanych klatek z~filmów \texttt{ai\_baseline} i~\texttt{adv\_fp\_trap},
stosując 5-krotną walidację stratified cross-validation oraz klasyfikator
\textit{Logistic Regression} z~regularyzacją L2 ($C=1{,}0$). Pełne manifesty
zbioru znajdują się w~plikach \texttt{dataset/manifest\_fake\_all.csv}
oraz \texttt{dataset/manifest\_real\_all.csv}.
Wynika z~tego, że benchmark końcowy nie jest w~pełni niezależny względem
tego komponentu; ograniczenie to~omówiono explicite w~sekcji
\ref{sec:threats_to_validity}.

Parametry fuzji zdefiniowane są w~pliku \texttt{fusion\_params.py};
wartości progu decyzji i~wagi heurystyk dobrano na~podstawie sweepu parametrów
oraz analizy frontu Pareto.
Wersje bibliotek dokumentowane są w~pliku \texttt{requirements.txt},
a~snapshoty wyników i~logi benchmarku zapisano w~repozytorium wynikowym projektu.

% -------------------------------------------------------------------
\section{Wnioski implementacyjne}

Przeprowadzona implementacja potwierdziła, że skuteczny detektor materiałów AI
nie powinien opierać się na~jednym sygnale. Znacznie lepsze rezultaty
daje połączenie kilku niezależnych źródeł informacji:
OCR, analizy ruchu, artefaktów częstotliwościowych,
proxy sygnałów związanych z~niewidocznym watermarkingiem i~metadanych proweniencji.
Doświadczenia z~budowy aplikacji pokazują jednocześnie,
że równie ważna jak warstwa algorytmiczna jest jakość
interfejsu, czytelność raportowania i~dostępność
artefaktów diagnostycznych --- szczególnie przy~analizie trudnych
przypadków, w~których automatyczna decyzja wymaga ludzkiej weryfikacji.

